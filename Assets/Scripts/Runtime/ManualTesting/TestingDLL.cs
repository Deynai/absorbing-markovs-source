using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Deynai.Markov
{
    public class TestingDLL : MonoBehaviour
    {
        private void Start()
        {
            //double[] data = new double[] { 1, 2, 3, 0, 1, 2, 0, 0, 1 };
            //Matrix matrix1 = new Matrix(data, 3, 3);

            //Matrix mult = matrix1 * matrix1;
            //Matrix mult2 = mult * matrix1;
            //Debug.Log($"{string.Join(",", mult2.Value)}");

            //Matrix m1pow = Matrix.Pow(matrix1, 3);
            //Debug.Log($"{string.Join(",", m1pow.Value)}");

            //Matrix id = Matrix.Identity(5);
            //Matrix id5 = Matrix.Pow(id, 5);
            //Debug.Log($"{string.Join(",", id5.Value)}");

            //double[] upperCorner = new double[] { 1, 0, 2, 0, 1, 0, 0, 0, 1 };
            //Matrix ucMatrix = new Matrix(upperCorner, 3, 3);

            //Matrix uc27 = Matrix.Pow(ucMatrix, 7);
            //Debug.Log($"{string.Join(",", uc27.Value)}");
        }
    }
}
