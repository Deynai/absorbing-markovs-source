using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Deynai.Markov
{
    public class TestCPPWebgl : MonoBehaviour
    {
        public void OnButtonClicked()
        {
            //double[] data = new double[] { 1, 2, 3, 0, 1, 2, 0, 0, 1 };
            //Matrix matrix1 = new Matrix(data, 3, 3);

            //Help.Notify(this, $"Successfully created new Matrix object");

            //Matrix mult = matrix1 * matrix1;

            //Help.Notify(this, $"Successfully multiplied Matrix");

            //Matrix m1pow = Matrix.Pow(matrix1, 3);

            //Help.Notify(this, $"Successfully run POW op");

            //Matrix id = Matrix.Identity(5);
            //Matrix id5 = Matrix.Pow(id, 5);

            //Help.Notify(this, $"Successfully run Identity op");

            //Help.Notify(this, $"Attempting to access value:");
            //Help.Notify(this, $"{string.Join(",", mult.Value)}");
        }
    }
}
